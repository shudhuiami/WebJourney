{
  "name": "webjourney-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "WebJourney — Interactive guidance on real websites",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev:demo": "npm run dev --workspace=@webjourney/demo-site",
    "dev:ext": "npm run dev --workspace=@webjourney/extension",
    "build:ext": "npm run build --workspace=@webjourney/extension",
    "package:ext": "npm run build:ext && powershell -Command \"New-Item -ItemType Directory -Force -Path release; Compress-Archive -Path apps/extension/dist/* -DestinationPath release/webjourney-v0.1.0.zip -Force\""
  },
  "devDependencies": {
    "happy-dom": "^20.14.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7"
  }
}
